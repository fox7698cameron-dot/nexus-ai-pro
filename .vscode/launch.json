{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: start (nexus-ai-pro)",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}